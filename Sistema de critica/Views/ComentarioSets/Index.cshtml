@model IEnumerable<Sistema_de_critica.ComentarioSet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.contenido_comentario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reseña.titulo_reseña)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.nombre_usuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.contenido_comentario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reseña.titulo_reseña)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.nombre_usuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdComentario }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdComentario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdComentario })
        </td>
    </tr>
}

</table>
